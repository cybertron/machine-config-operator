name: on-prem-dnsmasq.service
enabled: true
contents: |
  [Unit]
  Description=Run dnsmasq to provide local
  Before=kubelet.service crio.service
  [Service]
  ExecStart=/usr/sbin/dnsmasq -k --server=/{{ .DNS.Spec.BaseDomain }}/{{`{{.NonVirtualIP}}`}}#5333 -H /etc/api-int.host
